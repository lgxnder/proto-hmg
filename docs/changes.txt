AREAS

    an area's onEnd property may be used to trigger a flag after it has been 'beat'/'cleared'.
        A flag's state may be checked in a dialogue tree to drive a specific reward/action.

ADDITIONS

    - __dbggiveeverything()
    - __get_cat()
    - __heal()
    - ttl.__mountainchild
    - item.__sp4
    - item.__leatherwallet
    - chss.__quickstart
    - chss.__rockpath

CHANGES

    - moved the food stand to the marketplace menu
    - moved the cat to the rockpath (subject to change?)
    - higher sleep skill now means you can pass time quicker while sleeping
    - change d_loc() to uloc()
    - change ulst() to u_lst_loc();

MOUNTAIN START

    a new game path centered around sole usage of unarmed combat. Weapons are forbidden but,
        martial styles are able to be used in their place. New house + area.
